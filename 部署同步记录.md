# 部署同步记录

> **本文件作用：** 追踪本地开发与线上服务器之间的进度差异，包括 SQL 迁移的执行状态（本地/线上是否已执行）和代码部署状态。每次部署前后必须更新此文件，防止遗漏迁移或重复部署。

记录本地开发与线上服务器之间的进度差异，每次部署后更新。

## SQL 迁移执行状态

| 文件 | 说明 | 本地 DBeaver | 线上服务器 | 待执行 |
|------|------|:---:|:---:|:---:|
| `database/init.sql` | 创建 users 和 tasks 表 | ✅ | ✅ | |
| `database/02-add-due-date.sql` | tasks 表加 due_date | ✅ | ✅ | |
| `database/03-tree-structure.sql` | 重建为 items 表（树形结构） | ✅ | ✅ | |
| `database/04-add-no-prompt-flag.sql` | users 表加"不再提示"字段 | ✅ | ✅ | |
| `database/05-project-tag-restructure.sql` | 项目+标签+随笔+重复任务 | ✅ | ❌ | **是** |
| `database/06-soft-delete.sql` | 软删除（回收站功能） | ❌ | ❌ | **是** |

> **线上 SQL 执行方式：** DBeaver 连接线上数据库（122.51.29.69, todo_app），逐个执行未执行的文件，必须按顺序。

## 代码部署状态

| 改动 | 说明 | 已部署 | 方式 |
|------|------|:---:|------|
| 项目+标签+随笔+重复任务 | 后端接口 + 前端三栏改版 | ❌ | deploy.sh 上传代码 |
| 软删除 + 回收站 | 后端软删除 + 回收站 API + 前端回收站 UI | ❌ | deploy.sh 上传代码 |

> **代码部署方式：** 本地终端执行 `bash D:/SELF/self-project/CPToDo/deploy.sh`，自动上传前后端代码并重启 pm2。

## 下次部署清单

### 第一步：执行 SQL（线上 DBeaver）

1. 连接线上数据库 → 右键 `todo_app` → SQL 编辑器
2. 按顺序执行：
   - `05-project-tag-restructure.sql`
   - `06-soft-delete.sql`（回收站上线前必须先执行这个）

### 第二步：上传代码（本地终端）

```bash
bash D:/SELF/self-project/CPToDo/deploy.sh
```

一键搞定，脚本会自动上传前后端代码并重启后端。

### 第三步：验证

- 浏览器打开 http://122.51.29.69/login.html
- 测试：创建项目、添加任务、删除、打开回收站、恢复
