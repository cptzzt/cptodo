# 全栈 Todo 网站项目

## 项目背景

本项目是一个**学习项目**，目标是完成一个带登录注册功能的全栈 Todo 网站，同时理解前端、后端、数据库、服务器、部署的全栈原理。

产品定位：极简、可公开访问、支持用户登录注册的全栈 Todo 管理工具，聚焦"原理落地"而非"功能复杂"。

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML + CSS + JS | 原生实现，不依赖框架 |
| 后端 | Node.js + Express | 简化后端开发的框架 |
| 数据库 | MySQL | 关系型数据库，用 DBeaver 管理 |
| 服务器 | 阿里云/腾讯云 Linux | 手工配置环境、部署项目 |
| 部署 | 手动部署 | 不依赖 Vercel/Netlify |

## 项目阶段

当前阶段：**步骤 4 - 树形目录结构开发（后端已完成，迁移已执行，待测试前端）**

完整阶段：

1. ✅ **第一阶段** - 搭基础环境
2. ✅ **第二阶段** - 数据库搭建
3. ✅ **第三阶段** - 后端开发
4. 🔄 **第四阶段** - 前端三栏布局 + 树形目录（进行中）
5. ⏸ **第五阶段** - 服务器部署
6. ⏸ **第六阶段** - 问题排查与优化

## 数据库工具

- **DBeaver** - 用于执行 SQL 脚本和管理数据库（不是命令行 mysql）

## 当前需求：树形目录结构

### 核心概念

- **目录（folder）**：可嵌套目录或任务，无层级限制；无截止日期
- **任务（task）**：叶子节点，不能再添加子项；有截止日期和备注
- **两者都支持**：标题（title）、备注（notes）

### 数据库设计

```
users
├── id (PK)
├── username
└── password

items (统一存储目录和任务)
├── id (PK)
├── user_id (FK → users.id)
├── parent_id (FK → items.id, NULL=根级)
├── type ('folder' | 'task')
├── title
├── content (仅 task)
├── notes
├── due_date (仅 task)
├── completed (仅 task)
└── sort_order
```

**迁移记录：**
- `database/02-add-due-date.sql` - 已执行（tasks 表加 due_date）
- `database/03-tree-structure.sql` - **已通过 DBeaver 执行**（重建 items 表，tasks_old 已备份）
- 旧 tasks 数据已迁移至 items 表（type='task', title=content）

### 页面结构

```
┌─────────────┬──────────────────┬────────────────┐
│  左侧目录树  │   中间卡片列表    │   右侧详情面板   │
│  可展开折叠  │  当前目录下子项  │  编辑备注/日期   │
└─────────────┴──────────────────┴────────────────┘
```

### 添加逻辑（重要规则）

1. 选中目录 → 按钮文本"添加子目录/任务"，parent_id=当前目录
2. 未选中任何项 → 按钮文本"添加根目录/任务"，parent_id=NULL
3. 选中任务 → 按钮隐藏
4. **兜底**：即使按钮没隐藏，点击时判断选中项是否为任务，是则提示"任务下方无法添加新层级"并 return

### 删除逻辑

1. 无子项：直接 `confirm("确认删除「标题」？")`
2. 有子项（首次）：先 `confirm("该目录存在子项，确认删除？")`
3. 点击确认后：再 `confirm("确认删除？")` + 复选框"以后删除带有子项的目录时不再二次提示"
4. 勾选后：存到 localStorage，下次带子项目录只确认一次

### 接口设计

| 接口 | 方法 | 地址 | 说明 |
|------|------|------|------|
| 注册 | POST | /api/users/register | |
| 登录 | POST | /api/users/login | |
| 获取列表 | GET | /api/items | 返回扁平列表，前端构建树 |
| 创建项 | POST | /api/items | type+title+parent_id |
| 更新项 | PUT | /api/items/:id | 可更新 title/notes/content/due_date/completed |
| 删除项 | DELETE | /api/items/:id | CASCADE 删除子项 |

## Git 提交规范

`type: 简短描述`（feat / fix / refactor / docs / chore）

每次 commit message 由我提供。

## 步骤记录规范

每完成一个大步骤，在 `step/` 下创建 `.md` 文件。

## 文档规范

- `step/` — 开发阶段记录（做了什么、改了哪些文件）
- `流程手册/` — 操作手册（执行什么命令、在哪里执行、目的是什么）
- `database/` — SQL 迁移文件，每次数据库变更都单独建文件，不论大小
- scp 上传时目标路径必须明确写到最终文件夹名，不要依赖 scp 自动创建

## 严谨规范

- **不说没根据的数据** — 不确定的数字（硬盘大小、内存等）不要编造，说"不确定"或让用户自己查
- **写完整路径** — 不用 shorthand/简称，scp 命令、文件引用都写完整路径（`D:/SELF/.../backend` 而非 `backend`）
- **自己犯过的错误要记录** — 在流程手册中标注错误案例、原因、正确做法
- **不假设用户知道** — 命令在哪里执行、当前在哪个目录，每次都要标明

## 核心原则

1. **理解原理优先** - 每步提问都要追问"为什么"
2. **手动操作** - 不依赖一键生成/部署工具
3. **报错即学习** - 遇到报错先理解原因
4. **阶段复盘** - 每完成一个阶段复盘
5. **实时更新** - 新需求出现时立即更新 CLAUDE.md
6. **数据库操作** - 使用 DBeaver 执行 SQL，而非命令行
7. **SQL 迁移记录** - 每次数据库变更都在 `database/` 新建文件

## 深度学习准则

本项目不仅是 Todo 应用，更是深度学习全栈的桥梁项目。遵循六个词：

- **讨论** — 不盲从，遇到决策点先讨论利弊再动手
- **回答** — 提问后先让用户思考，再给解释，确保理解而非记住
- **优化** — 功能完成后追问"还能不能更好"，培养优化意识
- **严谨** — 不编造数据、不省略步骤、记录错误案例
- **深化** — 每个阶段结束后，延伸到真实工作场景的对比和思考
- **扩展** — 以当前项目为基础，逐步引入 Docker、CI/CD、HTTPS、框架重写等进阶主题

学习路线：手动部署（已完成）→ Docker 化 → CI/CD → 域名+HTTPS → 进阶后端（Java/Spring Boot 重写）
