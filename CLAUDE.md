# 全栈 Todo 网站项目

## 项目背景

本项目是一个**学习项目**，目标是完成一个带登录注册功能的全栈 Todo 网站，同时理解前端、后端、数据库、服务器、部署的全栈原理。

产品定位：极简、可公开访问、支持用户登录注册的全栈 Todo 管理工具，聚焦"原理落地"而非"功能复杂"。

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | React + Ant Design | 从原生 JS 迁移 |
| 后端 | Node.js + Express | 简化后端开发的框架 |
| 数据库 | MariaDB | 关系型数据库（MySQL 分支），用 DBeaver 管理 |
| 服务器 | 腾讯云 Debian 4核4G | 公网 IP：124.220.19.21（旧：122.51.29.69 已停用） |
| 域名 | cptodo.top | 已配置，未备案 |
| 部署 | deploy-react.sh | pm2 管理后端进程 |

## 项目阶段

当前阶段：**功能开发基本完成，准备转前端框架**

完整阶段：

1. ✅ **第一阶段** - 搭基础环境
2. ✅ **第二阶段** - 数据库搭建
3. ✅ **第三阶段** - 后端开发
4. ✅ **第四阶段** - 前端三栏布局
5. ✅ **第五阶段** - 服务器部署
6. ✅ **第六阶段** - 功能完善（项目+标签、回收站、批量删除、重复任务、已过期视图）
7. 🔄 **下一阶段** - 原生 JS 转前端框架（详见下方"前端框架迁移计划"）

## 当前功能概览

- **三栏布局**：左侧导航（随笔/今天/本周/项目/标签/重复任务/回收站/已过期）+ 中间卡片列表 + 右侧详情面板
- **项目/标签**：项目下挂任务和随笔，标签跨项目关联
- **重复任务**：独立视图，每天/每周周期，周期结束自动重建，不属任何项目
- **回收站**：软删除 + Tab 栏区分类型，恢复时自动连带恢复父项目
- **批量删除**：多选模式，重复任务不可批量删
- **已过期**：展示上周及之前未完成的非重复任务
- **数据库表结构**：见 `database/表结构说明.md`

## 前端框架迁移计划

### 背景

用户认为当前原生 JS + CSS 的界面样式像十年前的，不够好看、不够吸引人。希望通过转前端框架来改善 UI，同时学习前端框架的部署流程。

### 决策

- **框架选择：React**（用户已会 Vue，通过对比 Vue 学习 React）
- **分支策略：** main 分支冻结为原生 JS 版本，从 main 切 `feature/react-migration` 分支开发 React 版
- **迁移范围：** 前端完全重写，后端后续按需改动（邮箱/手机号登录等）
- **移动端适配、邮箱/手机号登录**均在 React 版中实现，原生版不再开发
- **学习方式：** 逐步迁移，每步类比 Vue 讲解 React 区别

### 详细进度

见 `版本追踪.md`

## 数据库工具

- **DBeaver** - 用于执行 SQL 脚本和管理数据库（不是命令行 mysql）

## 数据库结构

详见 `database/表结构说明.md`（每次数据库变更必须同步更新该文件）。

已执行的迁移：

| 文件 | 说明 | 本地 | 线上 |
|------|------|:---:|:---:|
| `database/init.sql` | 创建 users 和 tasks 表 | ✅ | ✅ |
| `database/02-add-due-date.sql` | tasks 表加 due_date | ✅ | ✅ |
| `database/03-tree-structure.sql` | 重建为 items 表 | ✅ | ✅ |
| `database/04-add-no-prompt-flag.sql` | users 表加字段 | ✅ | ✅ |
| `database/05-project-tag-restructure.sql` | 项目+标签+随笔+重复任务 | ✅ | ❌ |
| `database/06-soft-delete.sql` | 软删除（回收站） | ✅ | ❌ |

线上同步状态详见 `部署同步记录.md`。

## Git 提交规范

`type: 简短描述`（feat / fix / refactor / docs / chore）

每次 commit message 由 Claude 提供。

## 文档规范

- `CLAUDE.md` — **本文件**，项目全景、当前阶段、重大决策记录
- `版本追踪.md` — **各版本（原生 JS / React）的前端/后端/数据库进度总览，切换分支时必读**
- `需求记录.md` — 每次功能需求的追踪记录（用户原话、解决状态、日期）
- `step/` — 开发阶段记录（做了什么、改了哪些文件、踩了什么坑）
- `流程手册/` — 操作手册（执行什么命令、在哪里执行、目的是什么）
- `database/表结构说明.md` — 数据库表结构文档（随迁移更新）
- `部署同步记录.md` — 本地与线上服务器的进度差异
- `database/` — SQL 迁移文件，每次数据库变更都单独建文件
- scp 上传时目标路径必须明确写到最终文件夹名，不要依赖 scp 自动创建

### 文档维护规则

**以下情况必须检查并更新上述文档：**

1. 用户开始说新的方向/新话题时 → 检查 `需求记录.md` 是否需要新增条目
2. 出现信息不一致（如 CLAUDE.md 里的阶段描述和实际不符）→ 立即更新 `CLAUDE.md`
3. 有重大变动需求（如转框架、加新模块）→ 记录到 `CLAUDE.md` 和对应的 step/需求记录
4. 数据库结构变更 → 同步更新 `database/表结构说明.md`
5. 部署相关变更 → 同步更新 `部署同步记录.md`

**每个 md 文件头部应有简要说明：本文件为什么创建、作用是什么。**

## 需求工作流程

1. **先记录，后开发** - 用户提出功能需求时，先写进 `需求记录.md`，再开始编码
2. **状态标签**：
   - 🟢 完美解决 / ✅ 成功 - 一次搞定
   - 🟡 解决中 - 多轮调整
   - 🔴 阻塞 - 卡住了
   - ⏸ 暂停 - 暂时搁置
3. **简短记录** - 用户原话 + 解决状态，不用冗长分析

## 严谨规范

- **不说没根据的数据** — 不确定的数字（硬盘大小、内存等）不要编造，说"不确定"或让用户自己查
- **写完整路径** — 不用 shorthand/简称，scp 命令、文件引用都写完整路径（`D:/SELF/.../backend` 而非 `backend`）
- **自己犯过的错误要记录** — 在流程手册中标注错误案例、原因、正确做法
- **不假设用户知道** — 命令在哪里执行、当前在哪个目录，每次都要标明

## 核心原则

1. **理解原理优先** - 每步提问都要追问"为什么"
2. **手动操作** - 不依赖一键生成/部署工具
3. **报错即学习** - 遇到报错先理解原因
4. **阶段复盘** - 每完成一个阶段复盘
5. **实时更新** - 新需求出现时立即更新 CLAUDE.md
6. **数据库操作** - 使用 DBeaver 执行 SQL，而非命令行
7. **SQL 迁移记录** - 每次数据库变更都在 `database/` 新建文件
8. **记忆优先** - 重要的状态、决策、用户偏好必须写入记忆文件，确保新对话能无缝衔接。对话内的记忆是短期的，文件是长期的。宁可多写，不可遗漏。

## 接任务必读清单

每次接新需求时，**必须先阅读**以下文件再开始分析：

1. **CLAUDE.md** - 本文件，了解项目背景、技术栈、当前阶段
2. **版本追踪.md** - 确认当前在哪个分支、哪个版本、做到哪一步
3. **最新 SQL 迁移** - `database/` 目录下最新的 `.sql` 文件，了解当前数据结构
3. **需求记录.md** - 查看是否有进行中的需求，避免冲突
4. **database/表结构说明.md** - 了解当前完整的表结构
5. **memory/MEMORY.md** - 快速了解用户偏好和项目状态

**原因**：避免遗漏重要信息（如数据库新字段、新类型），确保方案基于最新代码

## 深度学习准则

本项目不仅是 Todo 应用，更是深度学习全栈的桥梁项目。遵循六个词：

- **讨论** — 不盲从，遇到决策点先讨论利弊再动手
- **回答** — 提问后先让用户思考，再给解释，确保理解而非记住
- **优化** — 功能完成后追问"还能不能更好"，培养优化意识
- **严谨** — 不编造数据、不省略步骤、记录错误案例
- **深化** — 每个阶段结束后，延伸到真实工作场景的对比和思考
- **扩展** — 以当前项目为基础，逐步引入 Docker、CI/CD、HTTPS、框架重写等进阶主题

学习路线：手动部署（已完成）→ **前端框架迁移** → Docker 化 → CI/CD → 域名+HTTPS → 进阶后端（Java/Spring Boot 重写）
