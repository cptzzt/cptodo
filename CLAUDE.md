# 全栈 Todo 网站项目

## 项目背景

本项目是一个**学习项目**，目标是完成一个带登录注册功能的全栈 Todo 网站，同时理解前端、后端、数据库、服务器、部署的全栈原理。

产品定位：极简、可公开访问、支持用户登录注册的全栈 Todo 管理工具，聚焦"原理落地"而非"功能复杂"。

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML + CSS + JS | 原生实现，不依赖框架，理解前后端通信本质 |
| 后端 | Node.js + Express | 简化后端开发的框架，聚焦接口逻辑 |
| 数据库 | MySQL | 经典关系型数据库，手工建表、写 SQL |
| 服务器 | 阿里云/腾讯云 Linux | 手工配置环境、部署项目 |
| 部署 | 手动部署 | 不依赖 Vercel/Netlify 等黑盒平台 |

## 项目阶段

当前阶段：**步骤 2 完成 - 前端页面搭建完成，等待 MySQL 初始化 + 后端启动**

完整阶段规划（来自 PRD）：

1. **第一阶段：前期准备** - 搭基础环境
2. **第二阶段：数据库搭建** - 手工建表，理解数据存储原理
3. **第三阶段：后端开发** - 编写接口，理解前后端通信
4. **第四阶段：前端开发** - 前后端联调
5. **第五阶段：服务器部署** - 外网访问
6. **第六阶段：问题排查与优化** - 自主解决问题

## Git 提交规范

每次提交使用 **Conventional Commits** 格式：

```
<type>: <简短描述>
```

**常用 type：**
- `feat` - 新功能
- `fix` - 修复 bug
- `refactor` - 重构（不改功能）
- `docs` - 文档
- `chore` - 构建/工具

**本次提交信息：**
```
feat: 后端基础架构搭建完成（用户注册登录、任务CRUD）
```

**后续每次提交，commit message 由我提供。**

## 步骤记录规范

每完成一个**大修改的一大步操作**，在 `step/` 文件夹下创建 `.md` 文件，记录：
- 做了什么事情
- 创建了哪些文件
- 步骤是什么
- 有哪些注意事项

文件名格式：`01-功能名称.md`

## 核心原则

1. **理解原理优先** - 每步提问都要追问"为什么"，不单纯复制代码
2. **手动操作** - 全程手工搭建，不依赖一键生成/部署工具
3. **报错即学习** - 遇到报错先理解原因，再找解决方案
4. **阶段复盘** - 每完成一个阶段，复盘"做了什么、核心原理、遇到的问题"
5. **步骤记录** - 每完成一大步，在 `step/` 下创建记录文件

## 用户意图

用户在 AI 辅助下完成这个全栈项目，核心目的是**通过这个项目打通全栈能力**，而不是仅仅得到一个 Todo 网站。AI 是"代码实现助手 + 原理解释工具"，用户主导需求拆解和架构设计。

## 数据库设计

- **用户表**：id（主键）、username、password（加密存储）
- **任务表**：id（主键）、user_id（关联用户）、content、completed

## 页面清单

1. 登录页 - 用户名/密码输入、登录按钮、注册跳转链接
2. 注册页 - 用户名/密码/确认密码输入、注册按钮、登录跳转链接
3. Todo 主页 - 任务输入框、新增按钮、任务列表（已完成/未完成）、修改/删除按钮、退出登录

## 接口设计

| 接口 | 方法 | 地址 | 说明 |
|------|------|------|------|
| 注册 | POST | /api/register | 用户名+密码，注册成功/失败 |
| 登录 | POST | /api/login | 用户名+密码，返回用户信息 |
| 查看任务 | GET | /api/tasks | 获取当前用户所有任务 |
| 新增任务 | POST | /api/tasks | 新增任务 |
| 修改任务 | PUT | /api/tasks/:id | 修改任务内容/状态 |
| 删除任务 | DELETE | /api/tasks/:id | 删除任务 |
